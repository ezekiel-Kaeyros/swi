'use client'

import { InfoWindow } from '@vis.gl/react-google-maps';



// this component is an example of how we can wrap a content with infowindow component
const LiveTrackingFilterInfoview = () => {
    return (
        <InfoWindow>
            Live tracking filter component goes here!!
        </InfoWindow>
    );
}

export default LiveTrackingFilterInfoview;